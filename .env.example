# Required: paste your Groq key after the equals sign. Do not commit a real key.
GROQ_API_KEY=
# Keep the model configurable so it can be changed without editing application code.
GROQ_MODEL=llama-3.3-70b-versatile
